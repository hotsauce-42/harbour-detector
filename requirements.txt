# Full local / dev install: every phase, the Streamlit GUI, and dev tooling.
# The container images install the narrower files instead:
#   Dockerfile.spark  -> requirements-spark.txt (base + pyspark + JVM)
#   Dockerfile.enrich -> requirements-base.txt  (no Spark, no JVM)
-r requirements-spark.txt

# GUI (app.py)
streamlit>=1.40.0        # st.segmented_control (map geometry toggle)
streamlit-folium>=0.20.0
folium>=0.16.0

# Dev tooling (not needed at runtime — see the Dockerfiles)
mypy>=1.11.0
